/**
 * Este paquete contiene las clases responsables de actuar como controladores dentro de la aplicación.
 * <p>
 * Los controladores coordinan la comunicación entre la capa de vista (UI) y las capas de servicio y repositorio.
 * Su propósito principal es manejar las interacciones del usuario, procesar las entradas, y actualizar las vistas
 * en función de los resultados obtenidos del modelo y la lógica de negocio.
 * <p>
 * Las clases en este paquete sirven como intermediarios clave, asegurando que la lógica de presentación se mantenga
 * separada de la lógica de negocio y el acceso a los datos, promoviendo una arquitectura limpia y siguiendo el
 * patrón MVC (Model-View-Controller).
 * <p>
 * De esta forma, el paquete `controller` ayuda a lograr un desacoplamiento efectivo de las distintas capas de la
 * aplicación, mejorando la mantenibilidad, la capacidad de prueba, y la flexibilidad del sistema.
 */
package ar.com.hmu.controller;
